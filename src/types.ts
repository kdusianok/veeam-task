export type Label = {
  text: string;
  left: number;
  top: number;
  editable: boolean;
};

export type Image = {
  src: string;
  width: number | string;
  height: number | string;
};

export type ImageSize = {
  width: number;
  height: number;
};
